spring.application.name=portal

spring.datasource.url=jdbc:mysql://localhost:3306/placement_portal
spring.datasource.username=root
spring.datasource.password=naman

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Keep JWT signing stable across restarts.
jwt.secret=portal-signing-secret-change-this-value-2026

# Basic platform accounts (used by AuthController)
auth.admin.username=admin
auth.admin.password=admin
auth.placement.username=placement1
auth.placement.password=admin